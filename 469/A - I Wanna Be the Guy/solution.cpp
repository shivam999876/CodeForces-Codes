#include <bits/stdc++.h>using namespace std; void solve() {    int p, q, n, x;    cin >> n;    cin >> p;    set <int> s;    for (int i = 0; i < p; i++){        cin >> x;        s.insert (x);    }    cin >> p;    for (int i = 0; i < p; i++){        cin >> x;        s.insert (x);    }    if (s.size() == n) cout << "I become the guy.\n";    else cout << "Oh, my keyboard!\n";} signed main(void){    std::ios::sync_with_stdio(false);    cin.tie(0); cout.tie(0);        int t = 1;     // cin >> t;    while (t--) solve();    return 0;    }