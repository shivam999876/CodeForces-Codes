#include <bits/stdc++.h>using namespace std;// #define int long long void solve() {    int n;    cin >> n;    int arr[n + 1], vis[n + 1] = {0};    for (int i = 1; i < n + 1; i++){        cin >> arr[i];    }    for (int i = 1; i < n + 1; i++){        vector <int> a, b;        for (int j = i; j < n + 1; j *= 2){            if (vis[j]) continue;            vis[j] = 1;            a.push_back (arr[j]);            b.push_back (j);        }        sort (a.begin(), a.end());        for (int j = 0; j < a.size(); j++){            arr[b[j]] = a[j];        }    }    for (int i = 1; i < n + 1; i++){        if (arr[i] != i){            cout << "NO\n";            return;        }    }    cout << "YES\n";} signed main(void){        // freopen("input.txt", "r", stdin);    // freopen("output.txt", "w", stdout);        std::ios::sync_with_stdio(false);    cin.tie(0); cout.tie(0);        int t = 1;     cin >> t;    while (t--) solve();    return 0;    }