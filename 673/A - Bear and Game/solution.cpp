#include <bits/stdc++.h>using namespace std;// #define int long long void solve() {    int n, x;    cin >> n;    int p = 0;    for (int i = 0; i < n; i++){        cin >> x;        if (x - p > 15){            cout << p + 15;            return;        }        else p = x;    }    cout << min (x + 15, 90);} signed main(void){         std::ios::sync_with_stdio(false);    cin.tie(0); cout.tie(0);        int t = 1;      while (t--) solve();    return 0;    }