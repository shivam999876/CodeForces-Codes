#include <bits/stdc++.h>using namespace std; void solve() {    int n, t, x;    cin >> n >> t;    for (int i = 1; i <= n; i++){        cin >> x;        t -= (86400 - x);        if (t <= 0){            cout << i;            return;        }    }} signed main(void){        // freopen("input.txt", "r", stdin);    // freopen("output.txt", "w", stdout);        std::ios::sync_with_stdio(false);    cin.tie(0); cout.tie(0);        int t = 1;     // cin >> t;    while (t--) solve();    return 0;    }