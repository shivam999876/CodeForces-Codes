#include <bits/stdc++.h>using namespace std; void solve() {    int k, n, s = 0;    cin >> k >> n;    for (int i = 0; i < 10; i++){        if (s % 10 == n || (s % 10 == 0 && i)){            cout << i;            return;        }        s += k;    }    cout << "10";} signed main(void){        // freopen("input.txt", "r", stdin);    // freopen("output.txt", "w", stdout);        std::ios::sync_with_stdio(false);    cin.tie(0); cout.tie(0);        int t = 1;     // cin >> t;    while (t--) solve();    return 0;    }